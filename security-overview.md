# Security Overview

**Status:** ✅ **RESOLVED** — All 14 manual-review findings fixed (or obsolete) as of 2026-06-12.

> Auto-generated by GitHub Actions · Last updated: 2026-06-07 23:46 UTC · Commit: `63b7bde`

## Summary

| Scanner | Findings |
|---------|----------|
| npm audit | 0 vulnerabilities (0 critical, 0 high, 0 moderate, 0 low) |
| Semgrep (OWASP / Node.js / Secrets) | 0 issues |

---

## Dependency Vulnerabilities (npm audit)

- No vulnerable packages found

---

## Static Analysis (Semgrep)

- Semgrep did not produce output (token may not be set)

---

## Manual Review Notes

The following issues were identified in a manual review and require human judgement to fix.
Update this section as issues are resolved.

| # | Severity | Location | Issue | Status |
|---|----------|----------|-------|--------|
| 1 | Critical | `server.js` | Free membership activation bypasses Stripe payment check | **Fixed** — `/api/portal/membership/subscribe` now only activates free tiers (`priceAud <= 0`); paid tiers return 402. Added the missing `/api/portal/membership/checkout` endpoint so paid tiers go through Stripe and activate via the webhook. |
| 2 | High | `server.js` | Forum post/thread edit missing ownership check | **Obsolete** — the in-house forum was replaced by external Discourse; port 8081 is now a redirect and no forum endpoints remain in `server.js`. |
| 3 | High | `server.js` | Admin forum user save matches by name; accepts raw `passwordHash` | **Obsolete** — endpoint removed with the forum (see #2). |
| 4 | High | `server.js` | Full repair board replaced from request body with no schema validation | **Fixed** — `/api/admin/repairs/save` validates the board shape (object with ≤50 column objects with string ids, each with ≤1000 card objects with string ids) before writing. |
| 5 | High | `server.js` | Password reset tokens stored as plaintext | **Fixed** — tokens are SHA-256-hashed before storage in `password-reset-tokens.db`; the raw token exists only in the email link. (Also fixed a crash: forgot-password referenced an undefined `username` variable, so the endpoint was broken; lookup is now by email only.) |
| 6 | High | `server.js` | Sellers can self-publish products without manager approval | **Fixed** — seller saves on `/api/admin/catalog/products/save` are always forced to `status: 'draft'`; only a manager can publish. |
| 7 | High | `server.js` | Order save overwrites total/payments with no audit log | **Fixed** — `/api/admin/orders/save` audit entries now record before/after order total and summed payments, attributable to the staff member. |
| 8 | Medium | `server.js` | Email change with no verification → account takeover via password reset | **Fixed** — `/api/portal/profile` email changes now require the current password, a valid format, uniqueness across accounts, and send a notification to the old address. |
| 9 | Medium | `server.js` | Stripe secret key and SMTP password written to plaintext `settings.db` | **Fixed** — env secrets are no longer copied into `settings.db` at startup (getters fall back to env vars); secrets are masked in all admin API responses (masked values are ignored on save); dashboard-entered secrets are encrypted at rest with AES-256-GCM when `SETTINGS_ENCRYPTION_KEY` is set. **Action:** set `SETTINGS_ENCRYPTION_KEY` in the production `.env`, and remove any previously-migrated plaintext secrets from `settings.db` (or re-save settings once the key is set). |
| 10 | Medium | `server.js` | Rate limit counters are in-memory — reset on process restart | **Fixed** — login attempts and public rate counters are persisted to `rate-limits.db` (lockouts flushed immediately, the rest on the 10-minute timer) and reloaded at startup. |
| 11 | Medium | `server.js` | No cart item count limit — computational DoS vector | **Fixed** — `/api/cart/save` and `/api/checkout` cap carts at 100 line items and quantities at 999. |
| 12 | Medium | `server.js` | No rate limit on membership subscribe/cancel endpoint | **Fixed** — `membership` rate-limit bucket (10/10 min) applied to subscribe, cancel and the new checkout endpoint. |
| 13 | Medium | `server.js` | Order tracking token response leaks customer name and email | **Fixed** — `/api/order-token` is rate-limited and returns only the first name and a masked email; registration sends the token and the server resolves the real email itself. |
| 14 | Medium | `server.js` | CSP includes `unsafe-inline` for scripts | **Fixed (admin/portal) / Accepted (public)** — admin and portal now get a strict CSP with no `unsafe-inline` for scripts (inline scripts allowed via SHA-256 hashes computed from the built HTML). The public/games/tools sites keep `unsafe-inline` because Google AdSense requires it; removing it there would break ad serving. |
